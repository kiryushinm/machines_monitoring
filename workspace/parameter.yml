# Parameter file for Machine State Monitoring Solution
# This file supports parameterization across different environments (DEV, TEST, PROD)
# For use with fabric-cicd library

find_replace:
  # Replace Eventhouse query service URI references
  - find_value: "(https://[a-zA-Z0-9-]+\\.[a-zA-Z0-9-]+\\.kusto\\.fabric\\.microsoft\\.com)"
    replace_value:
      _ALL_: "$items.Eventhouse.MachineMonitoringEH.$queryserviceuri"
    is_regex: "true"

key_value_replace:
  # Update workspace references in platform files
  - find_key: $.config.logicalId
    replace_value:
      _ALL_: "$workspace.$id"
    item_type: ["Eventhouse", "KQLDatabase"]
    file_path: "**/.platform"
